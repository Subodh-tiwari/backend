"use strict"

const config = {
    client : 'pg',
    connection : {
        host : 'localhost',
        user : 'root',
        password : 'prabodh@69',
        database : 'backend'
    },
    debug : true
}

export default config;
